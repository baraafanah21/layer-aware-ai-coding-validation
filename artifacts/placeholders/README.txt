Recovered artifact paths were not provided in the prompt.

This directory intentionally contains only a placeholder. If original run
artifacts are recovered later, place them in a documented artifact directory
and extend the manifest with real paths. Do not create synthetic artifacts.
